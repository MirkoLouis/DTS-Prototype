{
    "name": "dict/dts",
    "description": "Document Tracking System (Pure PHP)",
    "type": "project",
    "require": {
        "php": ">=8.0",
        "chillerlan/php-qrcode": "^6.0",
        "dompdf/dompdf": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "process-timeout": 0
    },
    "scripts": {
        "dev": "php scripts/dev.php",
        "worker": "php console.php",
        "proxy": "cloudflared tunnel --url http://localhost:8000",
        "corrupt": "php scripts/corrupt.php",
        "seed:dev": "node seed.js",
        "seed:prod": "node scripts/setup-production-db.js"
    }
}
